name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ── Build: Windows ──────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install SCons
        run: pip install scons

      # ── Restore caches ──────────────────────────────────────────────────────
      - name: Restore Godot cache
        uses: actions/cache/restore@v4
        with:
          path: .godot
          key: godot-windows-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}

      - name: Restore godot-cpp cache
        uses: actions/cache/restore@v4
        with:
          path: |
            Vital.extension/Vendor/godot/bin
            Vital.extension/.build_godotcpp.debug
            Vital.extension/.build_godotcpp.release
          key: godot-cpp-windows-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}

      - name: Restore Conan cache
        uses: actions/cache/restore@v4
        with:
          path: C:\Users\runneradmin\.conan2
          key: conan-windows-${{ hashFiles('Vital.sandbox/conanfile.*') }}
          restore-keys: conan-windows-

      - name: Restore Discord SDK cache
        uses: actions/cache/restore@v4
        with:
          path: Vital.sandbox/Vendor/discord-sdk
          key: discord-sdk-windows

      # ── Build ───────────────────────────────────────────────────────────────
      - name: Build Debug
        env:
          PYTHONUNBUFFERED: "1"
          CL: /MP
          CONAN_CPU_COUNT: "4"
          CMAKE_BUILD_PARALLEL_LEVEL: "4"
          GODOT_RENDERING_DRIVER: "null"
          GODOT_AUDIO_DRIVER: "Dummy"
        run: python build.py --all --debug --verbose || true

      - name: Verify Debug output
        shell: bash
        run: |
          found=$(find .dist/debug -name "*.exe" -o -name "*.x86_64" -o -name "*.app" 2>/dev/null | head -1)
          if [ -z "$found" ]; then
            echo "ERROR: Debug export produced no output files"
            exit 1
          fi
          echo "Debug output verified: $found"

      - name: Build Release
        env:
          PYTHONUNBUFFERED: "1"
          CL: /MP
          CONAN_CPU_COUNT: "4"
          CMAKE_BUILD_PARALLEL_LEVEL: "4"
          GODOT_RENDERING_DRIVER: "null"
          GODOT_AUDIO_DRIVER: "Dummy"
        run: python build.py --all --release --verbose --skip-extension || true

      - name: Verify Release output
        shell: bash
        run: |
          found=$(find .dist/release -name "*.exe" -o -name "*.x86_64" -o -name "*.app" 2>/dev/null | head -1)
          if [ -z "$found" ]; then
            echo "ERROR: Release export produced no output files"
            exit 1
          fi
          echo "Release output verified: $found"

      # ── Save caches (always, even on failure) ───────────────────────────────
      - name: Save Godot cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .godot
          key: godot-windows-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}

      - name: Save godot-cpp cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            Vital.extension/Vendor/godot/bin
            Vital.extension/.build_godotcpp.debug
            Vital.extension/.build_godotcpp.release
          key: godot-cpp-windows-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}

      - name: Save Conan cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: C:\Users\runneradmin\.conan2
          key: conan-windows-${{ hashFiles('Vital.sandbox/conanfile.*') }}

      - name: Save Discord SDK cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: Vital.sandbox/Vendor/discord-sdk
          key: discord-sdk-windows

      # ── Upload ──────────────────────────────────────────────────────────────
      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: dist-windows
          path: .dist/


  # ── Build: Linux ────────────────────────────────────────────────────────────
  # TODO: re-enable once Windows is confirmed working
  # build-linux:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with: { fetch-depth: 0, submodules: recursive }
  #     - uses: actions/setup-python@v5
  #       with: { python-version: "3.x" }
  #     - run: sudo apt-get update && sudo apt-get install -y scons gcc g++ libssl-dev
  #     - uses: actions/cache/restore@v4
  #       with: { path: .godot, key: "godot-linux-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}" }
  #     - uses: actions/cache/restore@v4
  #       with:
  #         path: |
  #           Vital.extension/Vendor/godot/bin
  #           Vital.extension/.build_godotcpp.debug
  #           Vital.extension/.build_godotcpp.release
  #         key: godot-cpp-linux-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}
  #     - uses: actions/cache/restore@v4
  #       with: { path: ~/.conan2, key: "conan-linux-${{ hashFiles('Vital.sandbox/conanfile.*') }}", restore-keys: "conan-linux-" }
  #     - uses: actions/cache/restore@v4
  #       with: { path: Vital.sandbox/Vendor/discord-sdk, key: discord-sdk-linux }
  #     - run: python3 build.py --all --debug --verbose || true
  #       env: { PYTHONUNBUFFERED: "1", CMAKE_BUILD_PARALLEL_LEVEL: "4", GODOT_RENDERING_DRIVER: "null", GODOT_AUDIO_DRIVER: "Dummy" }
  #     - run: python3 build.py --all --release --verbose --skip-extension || true
  #       env: { PYTHONUNBUFFERED: "1", CMAKE_BUILD_PARALLEL_LEVEL: "4", GODOT_RENDERING_DRIVER: "null", GODOT_AUDIO_DRIVER: "Dummy" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: .godot, key: "godot-linux-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with:
  #         path: |
  #           Vital.extension/Vendor/godot/bin
  #           Vital.extension/.build_godotcpp.debug
  #           Vital.extension/.build_godotcpp.release
  #         key: godot-cpp-linux-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: ~/.conan2, key: "conan-linux-${{ hashFiles('Vital.sandbox/conanfile.*') }}" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: Vital.sandbox/Vendor/discord-sdk, key: discord-sdk-linux }
  #     - uses: actions/upload-artifact@v4
  #       with: { name: dist-linux, path: .dist/ }


  # ── Build: macOS ────────────────────────────────────────────────────────────
  # TODO: re-enable once Windows is confirmed working
  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with: { fetch-depth: 0, submodules: recursive }
  #     - uses: actions/setup-python@v5
  #       with: { python-version: "3.x" }
  #     - run: pip install scons
  #     - uses: actions/cache/restore@v4
  #       with: { path: .godot, key: "godot-macos-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}" }
  #     - uses: actions/cache/restore@v4
  #       with:
  #         path: |
  #           Vital.extension/Vendor/godot/bin
  #           Vital.extension/.build_godotcpp.debug
  #           Vital.extension/.build_godotcpp.release
  #         key: godot-cpp-macos-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}
  #     - uses: actions/cache/restore@v4
  #       with: { path: ~/.conan2, key: "conan-macos-${{ hashFiles('Vital.sandbox/conanfile.*') }}", restore-keys: "conan-macos-" }
  #     - uses: actions/cache/restore@v4
  #       with: { path: Vital.sandbox/Vendor/discord-sdk, key: discord-sdk-macos }
  #     - run: python3 build.py --all --debug --verbose || true
  #       env: { PYTHONUNBUFFERED: "1", CMAKE_BUILD_PARALLEL_LEVEL: "4", GODOT_RENDERING_DRIVER: "null", GODOT_AUDIO_DRIVER: "Dummy" }
  #     - run: python3 build.py --all --release --verbose --skip-extension || true
  #       env: { PYTHONUNBUFFERED: "1", CMAKE_BUILD_PARALLEL_LEVEL: "4", GODOT_RENDERING_DRIVER: "null", GODOT_AUDIO_DRIVER: "Dummy" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: .godot, key: "godot-macos-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with:
  #         path: |
  #           Vital.extension/Vendor/godot/bin
  #           Vital.extension/.build_godotcpp.debug
  #           Vital.extension/.build_godotcpp.release
  #         key: godot-cpp-macos-${{ hashFiles('Vital.extension/Vendor/godot/version.py') }}
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: ~/.conan2, key: "conan-macos-${{ hashFiles('Vital.sandbox/conanfile.*') }}" }
  #     - if: always()
  #       uses: actions/cache/save@v4
  #       with: { path: Vital.sandbox/Vendor/discord-sdk, key: discord-sdk-macos }
  #     - uses: actions/upload-artifact@v4
  #       with: { name: dist-macos, path: .dist/ }


  # ── Release: Collect & Publish ──────────────────────────────────────────────
  release:
    needs: [build-windows]
    runs-on: ubuntu-latest

    steps:
      - name: Download all dist artifacts
        uses: actions/download-artifact@v4
        with:
          path: collected/

      - name: Package artifacts
        run: |
          TAG="${{ github.ref_name }}"
          cd collected
          for platform_dir in dist-windows dist-linux dist-macos; do
            platform="${platform_dir#dist-}"
            [ ! -d "$platform_dir" ] && continue
            for build_type in debug release; do
              [ ! -d "$platform_dir/$build_type" ] && continue
              for target_dir in "$platform_dir/$build_type"/*/; do
                target=$(basename "$target_dir")
                zip_name="../Vital.sandbox-${TAG}-${platform}-${target}-${build_type}.zip"
                zip -r "$zip_name" "$target_dir"
                echo "Packaged: $(basename $zip_name)"
              done
            done
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name:               ${{ github.ref_name }}
          name:                   ${{ github.ref_name }}
          generate_release_notes: true
          draft:                  false
          prerelease:             false
          files: "*.zip"